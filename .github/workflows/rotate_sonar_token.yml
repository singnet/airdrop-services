name: Rotate Sonar Token

on:
  schedule:
    - cron: "0 6 1 */3 *"
  workflow_dispatch:

jobs:
  rotate:
    uses: singnet/security-workflows/.github/workflows/sonar-token-rotation.yml@master
    secrets: inherit
