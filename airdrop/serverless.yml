service: airdropservices
frameworkVersion: "2"

provider:
  name: aws
  runtime: python3.8
  lambdaHashingVersion: 20201221
  memorySize: 512
  timeout: 30
  region: ${file(./config.${self:provider.stage}.json):REGION}
  stage: ${opt:stage,'dev'}
  deploymentBucket:
    name: snet-serverless-artifacts # Deployment bucket name. Default is generated by the framework
    serverSideEncryption: AES256 # when using server-side encryption
    tags: # Tags that will be added to each of the deployment resources
      key1: airdrop-service
  deploymentPrefix: serverless
  tracing: true

custom:
  pythonRequirements:
    fileName: airdrop/requirements.txt
    dockerizePip: true
    useDownloadCache: true
    useStaticCache: true
    cacheLocation: "/var/cache/serverless"
  prune:
    automatic: true
    includeLayers: true
    number: 5
  splitStacks:
    perFunction: false
    perType: true
    perGroupFunction: false

plugins:
  - serverless-offline
  - serverless-python-requirements
  - serverless-plugin-tracing
  - serverless-prune-plugin
  - serverless-plugin-split-stacks
  - serverless-latest-layer-version
  - serverless-aws-documentation

documentation:
  models:
    - name: "ErrorMessage"
      contentType: "application/json"
      schema: ${file(airdrop/documentation/models/error.json)}
    - name: "UserNotificationInput"
      contentType: "application/json"
      schema: ${file(airdrop/documentation/models/user_notification.json):UserNotificationInput}
    - name: "UserNotificationOutput"
      contentType: "application/json"
      schema: ${file(airdrop/documentation/models/user_notification.json):UserNotificationOutput}
    - name: "AirdropScheduleInput"
      contentType: "application/json"
      schema: ${file(airdrop/documentation/models/airdrop_schedule.json):AirdropScheduleInput}
    - name: "AirdropScheduleOutput"
      contentType: "application/json"
      schema: ${file(airdrop/documentation/models/airdrop_schedule.json):AirdropScheduleOutput}
    - name: "AirdropRegistrationInput"
      contentType: "application/json"
      schema: ${file(airdrop/documentation/models/airdrop_registration.json):AirdropRegistrationInput}
    - name: "AirdropRegistrationOutput"
      contentType: "application/json"
      schema: ${file(airdrop/documentation/models/airdrop_registration.json):AirdropRegistrationOutput}
    - name: "AirdropClaimStatusInput"
      contentType: "application/json"
      schema: ${file(airdrop/documentation/models/airdrop_claim_status.json):AirdropClaimStatusInput}
    - name: "AirdropClaimStatusOutput"
      contentType: "application/json"
      schema: ${file(airdrop/documentation/models/airdrop_claim_status.json):AirdropClaimStatusOutput}
    - name: "AirdropClaimInput"
      contentType: "application/json"
      schema: ${file(airdrop/documentation/models/airdrop_window_claim.json):AirdropClaimInput}
    - name: "AirdropClaimOutput"
      contentType: "application/json"
      schema: ${file(airdrop/documentation/models/airdrop_window_claim.json):AirdropClaimOutput}
    - name: "AirdropClaimHistoryInput"
      contentType: "application/json"
      schema: ${file(airdrop/documentation/models/airdrop_claim_history.json):AirdropClaimHistoryInput}
    - name: "AirdropClaimHistoryOutput"
      contentType: "application/json"
      schema: ${file(airdrop/documentation/models/airdrop_claim_history.json):AirdropClaimHistoryOutput}
    - name: "AirdropClaimStakeInput"
      contentType: "application/json"
      schema: ${file(airdrop/documentation/models/airdrop_claim_stake_details.json):AirdropClaimStakeInput}
    - name: "AirdropClaimStakeOutput"
      contentType: "application/json"
      schema: ${file(airdrop/documentation/models/airdrop_claim_stake_details.json):AirdropClaimStakeOutput}

package:
  patterns:
    - "!.circleci/**"
    - "!.gitignore/**"
    - "!.serverless/**"
    - "!requirements.txt"
    - "!venv/**"
    - "!serverless.yml"
    - "!test/**"
    - "!tests/**"
    - "!repository/**"
    - "!Readme.md"
    - "!package.json"
    - "!Dockerfile"
    - "!License"
    - "!node_modules/**"

functions:
  get_airdrop_schedules:
    warmup: true
    handler: airdrop/application/handlers/airdrop_handlers.get_airdrop_schedules
    role: ${file(./config.${self:provider.stage}.json):ROLE}
    vpc:
      securityGroupIds:
        - ${file(./config.${self:provider.stage}.json):SG1}
        - ${file(./config.${self:provider.stage}.json):SG2}
      subnetIds:
        - ${file(./config.${self:provider.stage}.json):VPC1}
        - ${file(./config.${self:provider.stage}.json):VPC2}
    events:
      - http:
          path: /airdrop-schedule/{token_address}
          method: get
          cors:
            origin: ${file(./config.${self:provider.stage}.json):ORIGIN}
            headers:
              - Content-Type
              - X-Amz-Date
              - Authorization
              - X-Api-Key
              - X-Amz-Security-Token
              - X-Amz-User-Agent
              - x-requested-with
          documentation:
            summary: "Get Airdrop schedule"
            description: "Get Airdrop schedule"
            tags:
              - "airdrop"
            requestHeaders:
              - name: "Content-Type"
                description: "application/json"
            requestModels:
              "application/json": "AirdropScheduleInput"
            methodResponses:
              - statusCode: "200"
                responseBody:
                  description: "Airdropschedule Data"
                  "application/json": "AirdropScheduleOutput"
                responseModels:
                  "application/json": "AirdropScheduleOutput"
              - statusCode: "400"
                responseModels:
                  "application/json": "ErrorMessage"
  user_registration:
    warmup: true
    handler: airdrop/application/handlers/airdrop_handlers.user_registration
    role: ${file(./config.${self:provider.stage}.json):ROLE}
    vpc:
      securityGroupIds:
        - ${file(./config.${self:provider.stage}.json):SG1}
        - ${file(./config.${self:provider.stage}.json):SG2}
      subnetIds:
        - ${file(./config.${self:provider.stage}.json):VPC1}
        - ${file(./config.${self:provider.stage}.json):VPC2}
    events:
      - http:
          path: /airdrop/registration
          method: post
          cors:
            origin: ${file(./config.${self:provider.stage}.json):ORIGIN}
            headers:
              - Content-Type
              - X-Amz-Date
              - Authorization
              - X-Api-Key
              - X-Amz-Security-Token
              - X-Amz-User-Agent
              - x-requested-with
          documentation:
            summary: "Airdrop user registration"
            description: "Airdrop user registration"
            tags:
              - "airdrop"
            requestHeaders:
              - name: "Content-Type"
                description: "application/json"
            requestModels:
              "application/json": "AirdropRegistrationInput"
            methodResponses:
              - statusCode: "200"
                responseBody:
                  description: "AirdropRegistration Data"
                  "application/json": "AirdropRegistrationOutput"
                responseModels:
                  "application/json": "AirdropRegistrationOutput"
              - statusCode: "400"
                responseModels:
                  "application/json": "ErrorMessage"
  user_eligibility:
    warmup: true
    handler: airdrop/application/handlers/airdrop_handlers.user_eligibility
    role: ${file(./config.${self:provider.stage}.json):ROLE}
    vpc:
      securityGroupIds:
        - ${file(./config.${self:provider.stage}.json):SG1}
        - ${file(./config.${self:provider.stage}.json):SG2}
      subnetIds:
        - ${file(./config.${self:provider.stage}.json):VPC1}
        - ${file(./config.${self:provider.stage}.json):VPC2}
    events:
      - http:
          path: /airdrop/user-eligibility
          method: post
          cors:
            origin: ${file(./config.${self:provider.stage}.json):ORIGIN}
            headers:
              - Content-Type
              - X-Amz-Date
              - Authorization
              - X-Api-Key
              - X-Amz-Security-Token
              - X-Amz-User-Agent
              - x-requested-with
          documentation:
            summary: "Airdrop user registration"
            description: "Airdrop user registration"
            tags:
              - "airdrop"
            requestHeaders:
              - name: "Content-Type"
                description: "application/json"
            requestModels:
              "application/json": "AirdropRegistration"
            methodResponses:
              - statusCode: "200"
                responseBody:
                  description: "AirdropRegistration Data"
                  "application/json": "AirdropRegistration"
                responseModels:
                  "application/json": "AirdropRegistration"
              - statusCode: "400"
                responseModels:
                  "application/json": "ErrorMessage"
  airdrop_window_claims:
    warmup: true
    handler: airdrop/application/handlers/airdrop_handlers.airdrop_window_claims
    role: ${file(./config.${self:provider.stage}.json):ROLE_WITH_SM}
    vpc:
      securityGroupIds:
        - ${file(./config.${self:provider.stage}.json):SG1}
        - ${file(./config.${self:provider.stage}.json):SG2}
      subnetIds:
        - ${file(./config.${self:provider.stage}.json):VPC1}
        - ${file(./config.${self:provider.stage}.json):VPC2}
    events:
      - http:
          path: /airdrop/claim
          method: post
          cors:
            origin: ${file(./config.${self:provider.stage}.json):ORIGIN}
            headers:
              - Content-Type
              - X-Amz-Date
              - Authorization
              - X-Api-Key
              - X-Amz-Security-Token
              - X-Amz-User-Agent
              - x-requested-with
          documentation:
            summary: "Airdrop window claims"
            description: "Airdrop window claims"
            tags:
              - "airdrop"
            requestHeaders:
              - name: "Content-Type"
                description: "application/json"
            requestModels:
              "application/json": "AirdropWindowClaim"
            methodResponses:
              - statusCode: "200"
                responseBody:
                  description: "Airdrop Window Claim get signed signature from the service"
                  "application/json": "AirdropWindowClaim"
                responseModels:
                  "application/json": "AirdropWindowClaim"
              - statusCode: "400"
                responseModels:
                  "application/json": "ErrorMessage"
  airdrop_window_claim_status:
    warmup: true
    handler: airdrop/application/handlers/airdrop_handlers.airdrop_window_claim_status
    role: ${file(./config.${self:provider.stage}.json):ROLE}
    vpc:
      securityGroupIds:
        - ${file(./config.${self:provider.stage}.json):SG1}
        - ${file(./config.${self:provider.stage}.json):SG2}
      subnetIds:
        - ${file(./config.${self:provider.stage}.json):VPC1}
        - ${file(./config.${self:provider.stage}.json):VPC2}
    events:
      - http:
          path: /airdrop/claim-txn
          method: post
          cors:
            origin: ${file(./config.${self:provider.stage}.json):ORIGIN}
            headers:
              - Content-Type
              - X-Amz-Date
              - Authorization
              - X-Api-Key
              - X-Amz-Security-Token
              - X-Amz-User-Agent
              - x-requested-with
          documentation:
            summary: "Airdrop window claim transaction status"
            description: "Airdrop window claim transaction status"
            tags:
              - "airdrop"
            requestHeaders:
              - name: "Content-Type"
                description: "application/json"
            requestModels:
              "application/json": "AirdropClaimStatus"
            methodResponses:
              - statusCode: "200"
                responseBody:
                  description: "Airdrop Window claim txn status update"
                  "application/json": "AirdropClaimStatus"
                responseModels:
                  "application/json": "AirdropClaimStatus"
              - statusCode: "400"
                responseModels:
                  "application/json": "ErrorMessage"
  airdrop_history:
    warmup: true
    handler: airdrop/application/handlers/airdrop_handlers.airdrop_window_claim_history
    role: ${file(./config.${self:provider.stage}.json):ROLE}
    vpc:
      securityGroupIds:
        - ${file(./config.${self:provider.stage}.json):SG1}
        - ${file(./config.${self:provider.stage}.json):SG2}
      subnetIds:
        - ${file(./config.${self:provider.stage}.json):VPC1}
        - ${file(./config.${self:provider.stage}.json):VPC2}
    events:
      - http:
          path: /airdrop-history
          method: post
          cors:
            origin: ${file(./config.${self:provider.stage}.json):ORIGIN}
            headers:
              - Content-Type
              - X-Amz-Date
              - Authorization
              - X-Api-Key
              - X-Amz-Security-Token
              - X-Amz-User-Agent
              - x-requested-with
          documentation:
            summary: "Airdrop window claim history"
            description: "Airdrop window claim history"
            tags:
              - "airdrop"
            requestHeaders:
              - name: "Content-Type"
                description: "application/json"
            requestModels:
              "application/json": "AirdropClaimHistory"
            methodResponses:
              - statusCode: "200"
                responseBody:
                  description: "Airdrop Window claim history"
                  "application/json": "AirdropClaimHistory"
                responseModels:
                  "application/json": "AirdropClaimHistory"
              - statusCode: "400"
                responseModels:
                  "application/json": "ErrorMessage"
  airdrop_user_notifications:
    warmup: true
    handler: airdrop/application/handlers/airdrop_handlers.user_notifications
    role: ${file(./config.${self:provider.stage}.json):ROLE}
    vpc:
      securityGroupIds:
        - ${file(./config.${self:provider.stage}.json):SG1}
        - ${file(./config.${self:provider.stage}.json):SG2}
      subnetIds:
        - ${file(./config.${self:provider.stage}.json):VPC1}
        - ${file(./config.${self:provider.stage}.json):VPC2}
    events:
      - http:
          path: /airdrop-notifications
          method: post
          cors:
            origin: ${file(./config.${self:provider.stage}.json):ORIGIN}
            headers:
              - Content-Type
              - X-Amz-Date
              - Authorization
              - X-Api-Key
              - X-Amz-Security-Token
              - X-Amz-User-Agent
              - x-requested-with
          documentation:
            summary: "Airdrop notification subscription"
            description: "Airdrop notification subscription"
            tags:
              - "airdrop"
            requestHeaders:
              - name: "Content-Type"
                description: "application/json"
            requestModels:
              "application/json": "UserNotificationInput"
            methodResponses:
              - statusCode: "200"
                responseBody:
                  description: "Airdrop notifications"
                  "application/json": "UserNotificationInput"
                responseModels:
                  "application/json": "UserNotificationOutput"
              - statusCode: "400"
                responseModels:
                  "application/json": "ErrorMessage"
  airdrop_claim_and_stake:
    warmup: true
    handler: airdrop/application/handlers/airdrop_handlers.get_claim_and_stake_details
    role: ${file(./config.${self:provider.stage}.json):ROLE}
    vpc:
      securityGroupIds:
        - ${file(./config.${self:provider.stage}.json):SG1}
        - ${file(./config.${self:provider.stage}.json):SG2}
      subnetIds:
        - ${file(./config.${self:provider.stage}.json):VPC1}
        - ${file(./config.${self:provider.stage}.json):VPC2}
    events:
      - http:
          path: /airdrop/claim-and-stake
          method: post
          cors:
            origin: ${file(./config.${self:provider.stage}.json):ORIGIN}
            headers:
              - Content-Type
              - X-Amz-Date
              - Authorization
              - X-Api-Key
              - X-Amz-Security-Token
              - X-Amz-User-Agent
              - x-requested-with
          documentation:
            summary: "Airdrop claim and stake details"
            description: "Airdrop claim and stake details"
            tags:
              - "airdrop"
            requestHeaders:
              - name: "Content-Type"
                description: "application/json"
            requestModels:
              "application/json": "AirdropClaimStakeInput"
            methodResponses:
              - statusCode: "200"
                responseBody:
                  description: "Airdrop notifications"
                  "application/json": "AirdropClaimStakeInput"
                responseModels:
                  "application/json": "AirdropClaimStakeOutput"
              - statusCode: "400"
                responseModels:
                  "application/json": "ErrorMessage"
  airdrop_event_listener:
    warmup: true
    handler: airdrop/application/handlers/airdrop_handlers.airdrop_event_listener
    role: ${file(./config.${self:provider.stage}.json):ROLE}
    vpc:
      securityGroupIds:
        - ${file(./config.${self:provider.stage}.json):SG1}
        - ${file(./config.${self:provider.stage}.json):SG2}
      subnetIds:
        - ${file(./config.${self:provider.stage}.json):VPC1}
        - ${file(./config.${self:provider.stage}.json):VPC2}
  airdrop_txn_watcher:
    warmup: true
    handler: airdrop/application/handlers/airdrop_handlers.airdrop_txn_watcher
    role: ${file(./config.${self:provider.stage}.json):ROLE}
    vpc:
      securityGroupIds:
        - ${file(./config.${self:provider.stage}.json):SG1}
        - ${file(./config.${self:provider.stage}.json):SG2}
      subnetIds:
        - ${file(./config.${self:provider.stage}.json):VPC1}
        - ${file(./config.${self:provider.stage}.json):VPC2}
    events:
      - schedule:
          rate: rate(2 minutes)
          name: ${file(./config.${self:provider.stage}.json):ENVIRONMENT}-airdrop_txn_watcher
          enabled: ${file(./config.${self:provider.stage}.json):airdrop_cron_status}
